---
layout: base.njk
title: "All Tags"
description: "Browse all blog post tags"
---

<article class="max-w-4xl mx-auto px-4 sm:px-6 lg:px-8 py-12">
    <!-- Tags Header -->
    <header class="mb-12">
        <div class="mb-6">
            <a href="/" class="text-accent-primary hover:text-accent-secondary transition-colors duration-200">
                ← Back to Blog
            </a>
        </div>
        
        <h1 class="text-4xl md:text-5xl font-bold text-text-primary mb-4">
            All Tags
        </h1>
        
        <p class="text-xl text-text-secondary leading-relaxed">
            Browse posts by topic and category
        </p>
    </header>
    
    <!-- Tags Grid -->
    <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-6">
        {% for tag in collections.tagList %}
        {% set tagPosts = collections.posts | postsByTag(tag) %}
        <div class="card hover:shadow-2xl transition-all duration-300 bg-gradient-to-br from-darcula-900/80 to-darcula-800/80 border border-logo-circuit/30 backdrop-blur-sm">
            <div class="mb-4">
                <h2 class="text-xl font-bold text-text-primary mb-2 hover:text-accent-primary transition-colors duration-200">
                    <a href="/tags/{{ tag | slug }}/">
                        {{ tag }}
                    </a>
                </h2>
                
                <p class="text-text-muted text-sm">
                    {{ tagPosts.length }} post{% if tagPosts.length !== 1 %}s{% endif %}
                </p>
            </div>
            
            <!-- Recent Posts Preview -->
            {% if tagPosts.length > 0 %}
            <div class="space-y-2">
                {% for post in tagPosts %}
                {% if loop.index <= 3 %}
                <div class="text-sm">
                    <a href="{{ post.url }}" class="text-accent-primary hover:text-accent-secondary transition-colors duration-200 line-clamp-1">
                        {{ post.data.title }}
                    </a>
                    <div class="text-text-muted text-xs">
                        {{ post.date | dateReadable }}
                    </div>
                </div>
                {% endif %}
                {% endfor %}
                
                {% if tagPosts.length > 3 %}
                <div class="text-sm text-text-muted">
                    +{{ tagPosts.length - 3 }} more post{% if (tagPosts.length - 3) !== 1 %}s{% endif %}
                </div>
                {% endif %}
            </div>
            {% endif %}
        </div>
        {% endfor %}
    </div>
    
    <!-- No Tags Message -->
    {% if collections.tagList.length === 0 %}
    <div class="text-center py-12">
        <p class="text-text-muted text-lg">No tags found.</p>
        <a href="/" class="text-accent-primary hover:text-accent-secondary transition-colors duration-200 mt-4 inline-block">
            ← Back to all posts
        </a>
    </div>
    {% endif %}
</article>
